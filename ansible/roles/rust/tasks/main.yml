---
- name: Download Rust installer
  get_url:
    url: https://sh.rustup.rs
    dest: /tmp/sh.rustup.rs
    mode: '0755'
    force: 'yes'

- name: Install Rust
  shell: |
    /tmp/sh.rustup.rs -y

- name: Source Cargo
  shell: |
    echo eport PATH="$HOME/.cargo/bin:$PATH" >> $HOME/.bashrc
